export const VALID_ROLES = ['SUPER_ADMIN', 'ADMIN', 'DEPT_HEAD', 'STUDENT', 'ACCOUNTING'];

export const ADMIN_CREATABLE_ROLES = ['DEPT_HEAD', 'STUDENT'];
// Exports:
//   ALLOWED_ROLES_BY_CREATOR  — map of which roles each requester role can create
//   REQUIRED_FIELDS_BY_ROLE   — per-role required fields (carnet, employeeNumber)
// TODO: implement in Commit 5 (migrate users).
