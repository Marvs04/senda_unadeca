# ─── Backend API ──────────────────────────────────────────────────────────────
# Optional in local dev when using Vite proxy (/api -> http://localhost:4000)
# Set it in production or if your API runs in another host/port.

# VITE_API_BASE_URL=http://localhost:4000
VITE_API_VERSION=v1
